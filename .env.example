# Get a free key at https://web3forms.com — use the same email as ECA’s inbox.
# Copy to `.env` locally and in your host’s env settings (Vite exposes only VITE_* vars to the client).
VITE_WEB3FORMS_ACCESS_KEY=
